# DocMind backend dependencies.
#
# NOTE: This host runs Python 3.14. The originally spec'd pins (e.g.
# PyMuPDF==1.24.0, pydantic-core via pydantic 2.7, chromadb==0.5.0,
# sentence-transformers==3.0.0) have no 3.14 wheels and fail to build,
# so the libraries below are unpinned and resolve to current 3.14-compatible
# releases. For an exact reproducible install, use requirements.lock.txt.

fastapi
uvicorn[standard]
pydantic-settings
pydantic[email]
sqlalchemy
python-multipart
python-jose[cryptography]
bcrypt
PyMuPDF
langchain-text-splitters
sentence-transformers
chromadb
httpx
aiofiles

# LLM runs via local Ollama at port 11434.
# Configured model lives in .env (OLLAMA_MODEL).
# qwen3.5:cloud requires a paid Ollama subscription; this host uses
# llama3.2:latest (local, CPU-forced via num_gpu=0 in services/llm.py).
