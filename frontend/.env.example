# Base URL the frontend uses to reach the backend API.
# - Native dev (npm run dev):   http://localhost:8000
# - Docker (nginx proxy):       /api   (set at build time, see docker-compose.yml)
VITE_API_URL=http://localhost:8000
